# MongoDB Atlas: URL-encode special characters in the password.
MONGO_URL=mongodb+srv://rdmfr59_db_user:<db_password>@cluster0.zygezd2.mongodb.net/?appName=Cluster0
DB_NAME=rdcloth
JWT_SECRET=change-me-to-a-secure-random-value
CORS_ORIGINS=http://localhost:3000
ENVIRONMENT=development
# Set true only for local tests without MongoDB. Atlas production must be false.
USE_MOCK_DB=false
